const Letters = (props) => {
    return (
        <h2>Choose From: {props.aplhLetter} <small>(alphabet letter)</small> or <small>(last letter of previous song)</small></h2>
    )
}

export default Letters;